# CleMoPI MQTT Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CLEMOPI ECOSYSTEM                            │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│  Mobile App      │  User scans QR code: "QR198676"
│  (Flutter)       │  
│  📱 QR Scanner   │
└────────┬─────────┘
         │ HTTP POST
         │ /api/v1/kickscooter/unlock
         │ Body: { "qrCode": "QR198676" }
         │ Auth: JWT + XSRF Token
         ▼
┌─────────────────────────────────────────────────────────────────┐
│  Backend API (Node.js + Express)                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 1. Authenticate user (JWT + XSRF)                        │   │
│  │ 2. Validate QR code exists in database                   │   │
│  │ 3. Check scooter availability                            │   │
│  └──────────────────┬───────────────────────────────────────┘   │
│                     ▼                                             │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ MQTT Service (mqtt.js)                                   │   │
│  │ ├─ Topic: "scooter/QR198676/command"                     │   │
│  │ ├─ Message: "unlock"                                     │   │
│  │ └─ QoS: 1 (at least once delivery)                       │   │
│  └──────────────────┬───────────────────────────────────────┘   │
│                     │                                             │
│  ┌──────────────────▼───────────────────────────────────────┐   │
│  │ MySQL Database Update                                    │   │
│  │ UPDATE kickscooters                                      │   │
│  │ SET lock_state = 'false'                                 │   │
│  │ WHERE qrCode = 'QR198676'                                │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────┬───────────────────────────────────────────┘
                      │ MQTT Publish
                      ▼
         ┌────────────────────────┐
         │  MQTT Broker           │
         │  (Mosquitto)           │
         │  📡 localhost:1883     │
         │                        │
         │  Topic Management:     │
         │  • scooter/+/command   │
         │  • scooter/+/status    │
         │  • scooter/+/battery   │
         └────────┬───────────────┘
                  │ MQTT Subscribe
                  ▼
    ┌─────────────────────────────────┐
    │  Physical Scooter Hardware      │
    │  (ESP32/Arduino + MQTT Client)  │
    │                                 │
    │  🛴 Scooter: QR198676          │
    │                                 │
    │  ┌───────────────────────────┐ │
    │  │ Subscribed to:            │ │
    │  │ scooter/QR198676/command  │ │
    │  └───────────┬───────────────┘ │
    │              ▼                  │
    │  ┌───────────────────────────┐ │
    │  │ Message Handler           │ │
    │  │ if (msg == "unlock")      │ │
    │  │   → Relay ON              │ │
    │  │   → Lock opens ✅         │ │
    │  │ if (msg == "lock")        │ │
    │  │   → Relay OFF             │ │
    │  │   → Lock closes 🔒        │ │
    │  └───────────────────────────┘ │
    └─────────────────────────────────┘


═══════════════════════════════════════════════════════════════════

                    MQTT TOPICS STRUCTURE

═══════════════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────────────┐
│  Command Topics (Backend → Hardware)                           │
├────────────────────────────────────────────────────────────────┤
│  scooter/QR198676/command    →  "lock" or "unlock"            │
│  scooter/QR198677/command    →  "lock" or "unlock"            │
│  scooter/+/command           →  (wildcard - all scooters)     │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│  Status Topics (Hardware → Backend) [Future Enhancement]       │
├────────────────────────────────────────────────────────────────┤
│  scooter/QR198676/status     →  "locked", "unlocked", "error" │
│  scooter/QR198676/battery    →  "85" (percentage)             │
│  scooter/QR198676/location   →  "33.5731,-7.5898" (GPS)       │
└────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════

                      DATA FLOW SEQUENCE

═══════════════════════════════════════════════════════════════════

1. User Action
   └─ Mobile app user scans QR code "QR198676"
      └─ App calls API: POST /kickscooter/unlock

2. Backend Processing
   └─ Verify JWT token ✓
      └─ Check XSRF token ✓
         └─ Query database for scooter ✓
            └─ Validate scooter available ✓

3. MQTT Communication
   └─ Publish to: scooter/QR198676/command
      └─ Payload: "unlock"
         └─ QoS: 1 (guaranteed delivery)

4. Hardware Response
   └─ ESP32 receives message
      └─ Activates GPIO pin (relay)
         └─ Lock mechanism opens
            └─ [Optional] Send status back

5. Database Update
   └─ UPDATE lock_state = 'false'
      └─ UPDATE lastOrderTime = NOW()
         └─ UPDATE scanStatus = 'Scanned'

6. User Notification
   └─ API returns success ✓
      └─ Mobile app shows "Unlocked!" ✅
         └─ Start ride timer
            └─ Enable navigation


═══════════════════════════════════════════════════════════════════

                    DEPLOYMENT ARCHITECTURE

═══════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│  Docker Compose Stack (docker-compose.yml)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   MySQL      │  │   Backend    │  │  Mosquitto   │         │
│  │   :3306      │  │   :4000      │  │   :1883      │         │
│  │              │  │              │  │   :9001 ws   │         │
│  │  ┌────────┐  │  │  ┌────────┐  │  │              │         │
│  │  │ Users  │  │  │  │ Express│  │  │  MQTT Broker │         │
│  │  │Scooters│◄─┼──┼──┤  API   ├──┼─►│  + Clients   │         │
│  │  │Rides   │  │  │  │ MQTT   │  │  │              │         │
│  │  └────────┘  │  │  └────────┘  │  └──────────────┘         │
│  └──────────────┘  └──────────────┘                            │
│         ▲                 ▲                    ▲                │
│         │                 │                    │                │
└─────────┼─────────────────┼────────────────────┼────────────────┘
          │                 │                    │
          │                 │                    │
┌─────────┴─────────────────┴────────────────────┴────────────────┐
│                     Network: clemopi_network                     │
└──────────────────────────────────────────────────────────────────┘
          ▲                 ▲                    ▲
          │                 │                    │
     Admin Panel       Mobile App          Scooter Hardware
     (Browser)         (Flutter)           (ESP32 + WiFi)


═══════════════════════════════════════════════════════════════════

                      SECURITY LAYERS

═══════════════════════════════════════════════════════════════════

Layer 1: API Authentication
├─ JWT Token (stored in httpOnly cookie)
├─ XSRF Token (in header)
└─ User session validation

Layer 2: Database Validation
├─ Verify scooter exists
├─ Check scooter status (not disabled)
└─ Validate user has active account

Layer 3: MQTT Security
├─ Internal network only (Docker/localhost)
├─ [Optional] Username/Password auth
├─ [Optional] TLS/SSL encryption
└─ Topic-level ACL (future)

Layer 4: Hardware Security
├─ Unique scooter ID per device
├─ Command validation
└─ Status reporting (anti-theft)


═══════════════════════════════════════════════════════════════════

                    TESTING CHECKLIST

═══════════════════════════════════════════════════════════════════

□ Install Mosquitto broker
  └─ macOS: brew install mosquitto && brew services start mosquitto

□ Start Backend API
  └─ cd Backend && npm install && npm start

□ Test MQTT connection
  └─ Run: ./test_mqtt.sh

□ Test API with curl
  └─ Login → Get token → Call unlock endpoint

□ Integrate with mobile app
  └─ Update QR scan handler to call API

□ Deploy hardware (ESP32)
  └─ Subscribe to scooter/{QR}/command topic

□ End-to-end test
  └─ Scan QR → API call → MQTT publish → Hardware receives → Lock opens


═══════════════════════════════════════════════════════════════════
